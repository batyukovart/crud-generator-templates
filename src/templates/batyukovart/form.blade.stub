%%formFieldsHtml%%

<div class="form-group">
    <div class="col-md-offset-4 col-md-4">
        <button type="submit" class="btn btn-primary">
                    {!! isset($submitButtonText) ? $submitButtonText : 'Сохранить' !!}
                </button>
    </div>
</div>
